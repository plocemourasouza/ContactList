<header>
  <!-- Fixed navbar -->
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
      <div class="container">
        <%= link_to 'Contact List', root_path, :class =>"navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <% if !user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Nova Conta', new_user_path, :class =>"nav-link" %>
              </li>
            <% end %>
            <li class="nav-item">
              <%= link_to 'Sobre', sobre_path, :class =>"nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Contato', contato_path, :class =>"nav-link" %>
            </li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if !user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Entrar', entrar_path, :class =>"nav-link" %>
              </li>
            <% else %>

              <li class="nav-item">
                <a href="#" class="nav-link">bem vindo, <%= current_user.name %></a>
              </li>
              <li class="nav-item">
                <%= link_to 'Minha Página', contacts_path, :class => "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to 'Editar', edit_user_path(current_user), :class => "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to 'Sair', sair_path, method: :delete, :class => "nav-link" %>
              </li>

            <% end %>
          </ul>
        </div>
      </div>
    </nav>

</header>